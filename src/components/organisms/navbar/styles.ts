import styled from 'styled-components';

export const NavStyled = styled.div`
  ul {
    max-width: 100%;
    height: 100%;
    display: flex;
    justify-content: flex-end;
    position: relative;
    gap: 16px;
  }
`;
